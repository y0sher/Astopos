.build/
Astopos.app/
*.dmg
dmg-stage/
*.xcodeproj
.DS_Store
.swiftpm/
# Raw source art (the compiled Astopos.icns is committed instead; README/release
# images are hosted on the GitHub release, not in the repo).
images/
# Per-release scratch notes consumed by `make release`.
RELEASE_NOTES.md
